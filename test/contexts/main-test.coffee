require '../spec-helper'

describe "contexts/main", ->
  Main = require '../../lib/contexts/main'
  describe '#initState', ->
    it 'returns initial state' #, ->
      # TODO stub database

  describe '#expandComponentProps', ->
    it 'returns templateProps' #, ->
      # TODO stub database
